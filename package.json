{
  "name": "blaze-script",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:w": "tsc --watch && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.9.0"
  },
  "dependencies": {
    "dexie": "^3.2.2",
    "html-escaper": "^3.0.3",
    "lodash.isequalwith": "^4.4.0"
  }
}
